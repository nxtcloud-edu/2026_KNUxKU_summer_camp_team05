{
  "name": "@tm/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "에이전트·라운드·데이터 계층이 공유하는 타입과 런타임 스키마",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "zod": "3.23.8"
  }
}
