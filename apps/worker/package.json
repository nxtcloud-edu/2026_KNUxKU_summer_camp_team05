{
  "name": "@tm/worker",
  "version": "0.0.0",
  "private": true,
  "description": "RunController Worker — 5역할 호출 순서, 상태 상한, 계약 버전과 migration 실행 경로 관리",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "smoke": "tsx src/smoke.ts",
    "mvp:fixture": "node --import tsx src/canonical-fixture-demo.ts",
    "mvp:provider-smoke": "node --import tsx src/canonical-provider-smoke.ts",
    "mvp:legacy-fixture": "node --import tsx src/mvp-fixture-demo.ts",
    "test": "node --import tsx --test src/orchestrator/finalize.test.ts src/orchestrator/state-ceiling.test.ts src/codex-gateway.test.ts src/mvp-fixture-run.test.ts src/canonical-fixture-run.test.ts src/canonical-live-run.test.ts src/canonical-component-adapters.test.ts"
  },
  "dependencies": {
    "@tm/agents": "*",
    "@tm/contracts": "*",
    "@tm/core": "*",
    "@tm/data-agents": "*",
    "@tm/db": "*",
    "bullmq": "5.28.1",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "tsx": "4.19.1",
    "typescript": "~5.8.3"
  }
}
